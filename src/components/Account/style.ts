import styled from 'styled-components';

export const AccountWrapper = styled.div`
  h1 {
    width: 25%;
    margin: auto;
    font-weight: bold;
    position: relative;
    color: #383f54 !important;
  }
  .cardHeight {
    height: auto;
  }
`;
