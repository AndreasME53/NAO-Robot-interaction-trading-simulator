import { IAction } from 'types';

const initialState = {
  userState: {}
};

function reducer(state = initialState, action: IAction) {
  switch (action.type) {
    default:
  }
  return state;
}

export default reducer;
